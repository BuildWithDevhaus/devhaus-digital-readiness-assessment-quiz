//import { AnimationItem } from 'lottie-web';
import App from 'src/types/app';

export default function onShowConfetti(app: App) {
  // const animWrapper = app.animWrapper as HTMLElement;
  // const animConfetti = app.animConfetti as AnimationItem;
  // animWrapper.style.display = 'block';
  // animConfetti.goToAndPlay(0);
  // const duration = animConfetti.getDuration();
  // setTimeout(() => {
  //   animWrapper.style.display = 'none';
  // }, duration * 1000);
}
